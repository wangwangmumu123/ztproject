<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="fun.cyhgraph.mapper.CartMapper">
    <insert id="insertBatch">
        insert into cart (name, pic, user_id, product_id,  number, amount, create_time) VALUES
        <foreach collection="cartList" item="cart" separator=",">
            (#{cart.name}, #{cart.pic}, #{cart.userId}, #{cart.productId}, #{cart.number}, #{cart.amount}, #{cart.createTime})
        </foreach>
    </insert>

    <delete id="deleteByProductId">
        delete from cart
        <where>
            <if test="productId != null">
                and product_id = #{productId}
            </if>

        </where>
    </delete>

    <select id="list" resultType="fun.cyhgraph.entity.Cart">
        select * from cart
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="productId != null">
                and product_id = #{productId}
            </if>

        </where>
        order by create_time desc
    </select>

</mapper>